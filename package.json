{
	"name": "engagesphere",
	"version": "1.0.0",
	"description": "A modern real-time chat application with video calls and file sharing",
	"main": "backend/server.js",
	  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "npm run install:all && npm run build:frontend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "render-build": "npm run install:all && npm run build:frontend",
    "render-start": "cd backend && npm start",
    "vercel-build": "npm run build:frontend",
    "vercel-dev": "npm run dev"
  },
	"keywords": [
		"chat",
		"real-time",
		"video-calls",
		"messaging",
		"react",
		"nodejs",
		"socketio"
	],
	"author": "Anshuman Singh",
	"license": "MIT",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"cookie-parser": "^1.4.6",
		"dotenv": "^16.4.1",
		"express": "^4.18.2",
		"jsonwebtoken": "^9.0.2",
		"mongoose": "^8.1.1",
		"socket.io": "^4.7.4"
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	},
	"engines": {
		"node": ">=16.0.0",
		"npm": ">=8.0.0"
	}
}
